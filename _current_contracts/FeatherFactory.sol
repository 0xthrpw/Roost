/*
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@%, .***.*@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&/. .,*****, *@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@*...*********,,,&@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&%.////**********,..&@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&/,..//(((((/......../.,%@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&/..(((((#%##((/......../(.,&@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&((/(#####%%(((((......../@@(.,%@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@%,///##%%&&%#(###(******(@%(/..//&@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@&@@@&.,(((#%%&&&%##%&#(##.///#(((//((#/%@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@&. (@&*.,###%&&&%%#%&&/.%%&%%(/(////(((((/@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@&*,,.%&(.,&&&&&%%#((%@#*%&&&&&%%###(//((((*&@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@&&(*..*%#,/@&%%%%(/./(%(&%&&&&&&&%%##///(((./@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@&,*%%(. ,#%,/%(/#%((///%(%%#%&&&&&&&%##((((((*.#@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@&(//((##(./#.(#/.(#////%%/((((#%&&&&&&&%####((,*#@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@&**(/(###%(/#//////////#&#***,,/#&&&&&&&&%##((/.,(@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@#./(((%%%%#(#*.////./*(#&(**,.../%&&&&&&&%#(((///%@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@&,.//%&@&%&///.////...,#%*.....//#@&@@&&&&&%%##*,&@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@/.,*(*#&&@%#(//////.. *&*,./...,*(%&&&&&&&&%%/.(@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@%,,./#*(&@&(((////*. .##,/.**../(#%%&&&%%%&#,,&@@@@@@@@@##@@@@@@@
@@@@@@@@@@@@@@@@%*,,/(./@&(/////(*  /&/.*****./(#&&%&&%(,/&@@@@@@%(##%@@@@@@@@@@
@@@@@@@@@@@@@@@@@&*..*#/.#%#/////* .##.*****./(#&##%%/,(@@@@&#(**(%@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@( /(#***(#////,.,%*.,****./%#(##* /@@&%/./,/%&@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@&@@@%*#*(/,.////.. /#.,******((((*.,&#*.*(,,#&@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@*,&@@&/#(./..///...%(,/.*****.//. ,, *%%,/%@@@@@%%@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@%#,/@@&#*#.////.,.,#/.********. ../#&%*#%&@@%((,../@@@@@@@@@@@@@
@@@@@@@@@@@@@%/(###*.#@&(./(///,. /%*,*,*,**,..,(#&(*(%%%//%%,(%/,@@@@@@@@@@@@@@
@@@@@@@@@@@@@%,/%####(,(#/..///,.,&#..*,,,,*,,./%/.//,.,%@&&&%#(,#@@@@@@@@@@@@@@
@@@@@@@@@@@@@&(/.*#%&&%#*,/.*,,,.*&/.,/,.. .*,..//,,*(&&%%&%#(/.#@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@/(##*,/#%&&#//.,,,,#%,.**,. .,,***.//#&&&&&&&#/(&@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@%&##%%%#**(#%%(*,. .&( .,.....,,,./#%&&&&&&&#//%@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@..(%%%%%%/.,./.,,.,&, ...,,,,/(((((%&&&&%#* (%((,%@@@@@@@@@@@@@@@@
@@@@@@@@@@@&#&@#*.,(#%%%#*..,.  /%   .,,,,,./(((#%#((*. ...,#/%@@@@@@@@@@@@@@@@@
@@@@@@@@@@@##, *(#(***((##/,    ((  .,,,,,,*./((*... ,/......(@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@#.  ,*, ,.///(///.. .%(  .,,,*,,**,,..,***,,....%@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@(.,.,,.   ..,,.//., .%/  ..,***....,././///.,/%,*&@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@/.,,//,..**  .,,,..*&/ .,,,,,,...////(//((/.  /@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@&(/./#///((*.       *%* ..,,,,,.,./////.*,... (@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@%,.,(#%#((#(.    ../%/ ..,,,...,*******,,.. (@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@&..,#%#%%#(/..   ,/#, ..,,...,,,,*****,,,/%@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@/. .,(%#%%(,    ,((. ........,,,.///..*,(@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@...   ,.(/./,   .((. ,......,,./((/...,&@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@#./,,.  .,//.*  .((..,.....,.////,. .#@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@#.,(/.*,. ./(,  .(#..*,...,****,,..,%@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@%*.,((/(/,*...  /#.,,..,,,*(*,...(@@@@@&#@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@&(*./#(#%((*,,./%, . ....,...,#@@@@@&%(#@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@&&@@@@@#,/#%##%%%/,,/&/  ....., ,#@@@@@&&%(,#@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@#,/%&%@@&/.(%%%%%%%//&( ... ../@@@@@&/(%%/.%@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@#/&#%##&&%//&%%%##(,##.....,%@&&%///%%%#(/,(@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@&./&&#(((#%(#%%%%(,*%..../##**#&@&&%((#(/(&@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@%*,(&@#%&#&%&&%#**,#..,//(#&&&&@@&(#%%%#/&@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@&&#/.%&&&@&&&&%%//,((.,/((##&&@%%&&&%(*#@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@&&%%%%&&@@@&&@&&&%%#//(%,,*(#%%&&&&&&&%/#@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@&&&@@@@@@@&&&&&%#/,,%////(%&&&&&&&(%@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@#/.(&&&&%&&&&@@&&&&&&&&%##*.%//(/(%%##%&%#%##((#%&@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@&%%%%%&&&&&&&&&@&&%%%%#*.#//##%&@&&@@@@@&&&&&&&@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@&(###%%&&@@&&&@@&&%%%(, /(./#%&&%(/(%%%&&@@&%##(&@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@&&%%%%%%&&&&&@&@&%%#(..#//#&&&&@&&%%&&&%%&@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@&&@@@&@&&&@&@@@@&%#(#,*%(##%&&@@@@@&&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@&#&@&&&&&&&%%(#(/&#%%%&&&&&&%&@&%&@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@%%&&%%%%#%%&/.&&#%&&%%&%#&&%@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@&&&##&&@%&#%&#./&#%##&@@@@@@@&&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@%&%%(#%&%**%#%#%@@@@@@@@@@@&@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@&&/&&@@%/&%&&&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@%&@@@@@%%&(@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@%@@@@@@@#&%&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@&@@@@@@@@%@%@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@&@@@@@@@@@@%&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&&%@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&&&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&&%&@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@&%#@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@(#@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
*/

pragma solidity ^0.6.6;

abstract contract Context {
    function _msgSender() internal virtual view returns (address payable) {
        return msg.sender;
    }

    function _msgData() internal virtual view returns (bytes memory) {
        this;
        return msg.data;
    }
}

interface IERC20 {

    function totalSupply() external view returns (uint256);

    function balanceOf(address account) external view returns (uint256);

    function transfer(address recipient, uint256 amount)
        external
        returns (bool);

    function allowance(address owner, address spender)
        external
        view
        returns (uint256);

    function approve(address spender, uint256 amount) external returns (bool);

    function transferFrom(
        address sender,
        address recipient,
        uint256 amount
    ) external returns (bool);

    function mint(address _to, uint256 _amount) external;

    event Transfer(address indexed from, address indexed to, uint256 value);

    event Approval(
        address indexed owner,
        address indexed spender,
        uint256 value
    );
}

library SafeMath {

    function add(uint256 a, uint256 b) internal pure returns (uint256) {
        uint256 c = a + b;
        require(c >= a, "SafeMath: addition overflow");

        return c;
    }

    function sub(uint256 a, uint256 b) internal pure returns (uint256) {
        return sub(a, b, "SafeMath: subtraction overflow");
    }

    function sub(
        uint256 a,
        uint256 b,
        string memory errorMessage
    ) internal pure returns (uint256) {
        require(b <= a, errorMessage);
        uint256 c = a - b;

        return c;
    }

    function mul(uint256 a, uint256 b) internal pure returns (uint256) {
        if (a == 0) {
            return 0;
        }

        uint256 c = a * b;
        require(c / a == b, "SafeMath: multiplication overflow");

        return c;
    }

    function div(uint256 a, uint256 b) internal pure returns (uint256) {
        return div(a, b, "SafeMath: division by zero");
    }

    function div(
        uint256 a,
        uint256 b,
        string memory errorMessage
    ) internal pure returns (uint256) {
        require(b > 0, errorMessage);
        uint256 c = a / b;
        return c;
    }

    function mod(uint256 a, uint256 b) internal pure returns (uint256) {
        return mod(a, b, "SafeMath: modulo by zero");
    }

    function mod(
        uint256 a,
        uint256 b,
        string memory errorMessage
    ) internal pure returns (uint256) {
        require(b != 0, errorMessage);
        return a % b;
    }
}

interface NestControlMigrator {
    function migrate(IERC20 token) external returns (IERC20);
}

library Address {

    function isContract(address account) internal view returns (bool) {

        uint256 size;
        // solhint-disable-next-line no-inline-assembly
        assembly {
            size := extcodesize(account)
        }
        return size > 0;
    }

    function sendValue(address payable recipient, uint256 amount) internal {
        require(
            address(this).balance >= amount,
            "Address: insufficient balance"
        );

        // solhint-disable-next-line avoid-low-level-calls, avoid-call-value
        (bool success, ) = recipient.call{value: amount}("");
        require(
            success,
            "Address: unable to send value, recipient may have reverted"
        );
    }

    function functionCall(address target, bytes memory data)
        internal
        returns (bytes memory)
    {
        return functionCall(target, data, "Address: low-level call failed");
    }

    function functionCall(
        address target,
        bytes memory data,
        string memory errorMessage
    ) internal returns (bytes memory) {
        return _functionCallWithValue(target, data, 0, errorMessage);
    }

    function functionCallWithValue(
        address target,
        bytes memory data,
        uint256 value
    ) internal returns (bytes memory) {
        return
            functionCallWithValue(
                target,
                data,
                value,
                "Address: low-level call with value failed"
            );
    }

    function functionCallWithValue(
        address target,
        bytes memory data,
        uint256 value,
        string memory errorMessage
    ) internal returns (bytes memory) {
        require(
            address(this).balance >= value,
            "Address: insufficient balance for call"
        );
        return _functionCallWithValue(target, data, value, errorMessage);
    }

    function _functionCallWithValue(
        address target,
        bytes memory data,
        uint256 weiValue,
        string memory errorMessage
    ) private returns (bytes memory) {
        require(isContract(target), "Address: call to non-contract");

        // solhint-disable-next-line avoid-low-level-calls
        (bool success, bytes memory returndata) = target.call{value: weiValue}(
            data
        );
        if (success) {
            return returndata;
        } else {
            if (returndata.length > 0) {
                // solhint-disable-next-line no-inline-assembly
                assembly {
                    let returndata_size := mload(returndata)
                    revert(add(32, returndata), returndata_size)
                }
            } else {
                revert(errorMessage);
            }
        }
    }
}

library SafeERC20 {
    using SafeMath for uint256;
    using Address for address;

    function safeTransfer(
        IERC20 token,
        address to,
        uint256 value
    ) internal {
        _callOptionalReturn(
            token,
            abi.encodeWithSelector(token.transfer.selector, to, value)
        );
    }

    function safeTransferFrom(
        IERC20 token,
        address from,
        address to,
        uint256 value
    ) internal {
        _callOptionalReturn(
            token,
            abi.encodeWithSelector(token.transferFrom.selector, from, to, value)
        );
    }

    function safeApprove(
        IERC20 token,
        address spender,
        uint256 value
    ) internal {
        require(
            (value == 0) || (token.allowance(address(this), spender) == 0),
            "SafeERC20: approve from non-zero to non-zero allowance"
        );
        _callOptionalReturn(
            token,
            abi.encodeWithSelector(token.approve.selector, spender, value)
        );
    }

    function safeIncreaseAllowance(
        IERC20 token,
        address spender,
        uint256 value
    ) internal {
        uint256 newAllowance = token.allowance(address(this), spender).add(
            value
        );
        _callOptionalReturn(
            token,
            abi.encodeWithSelector(
                token.approve.selector,
                spender,
                newAllowance
            )
        );
    }

    function safeDecreaseAllowance(
        IERC20 token,
        address spender,
        uint256 value
    ) internal {
        uint256 newAllowance = token.allowance(address(this), spender).sub(
            value,
            "SafeERC20: decreased allowance below zero"
        );
        _callOptionalReturn(
            token,
            abi.encodeWithSelector(
                token.approve.selector,
                spender,
                newAllowance
            )
        );
    }


    function _callOptionalReturn(IERC20 token, bytes memory data) private {
        bytes memory returndata = address(token).functionCall(
            data,
            "SafeERC20: low-level call failed"
        );
        if (returndata.length > 0) {
            // solhint-disable-next-line max-line-length
            require(
                abi.decode(returndata, (bool)),
                "SafeERC20: ERC20 operation did not succeed"
            );
        }
    }
}

contract Ownable is Context {
    address private _owner;

    event OwnershipTransferred(
        address indexed previousOwner,
        address indexed newOwner
    );

    constructor() internal {
        address msgSender = _msgSender();
        _owner = msgSender;
        emit OwnershipTransferred(address(0), msgSender);
    }

    function owner() public view returns (address) {
        return _owner;
    }

    modifier onlyOwner() {
        require(_owner == _msgSender(), "Ownable: caller is not the owner");
        _;
    }

    function renounceOwnership() public virtual onlyOwner {
        emit OwnershipTransferred(_owner, address(0));
        _owner = address(0);
    }

    function transferOwnership(address newOwner) public virtual onlyOwner {
        require(
            newOwner != address(0),
            "Ownable: new owner is the zero address"
        );
        emit OwnershipTransferred(_owner, newOwner);
        _owner = newOwner;
    }
}

contract TokenRecover is Ownable {
    function recoverERC20(address tokenAddress, uint256 tokenAmount) public onlyOwner {
        IERC20(tokenAddress).transfer(owner(), tokenAmount);
    }
}

contract FeatherFactory is Ownable, TokenRecover {
    using SafeMath for uint256;
    using SafeERC20 for IERC20;

    struct Avian {
        uint256 amount;
        uint256 mintDebt;
    }

    struct NestData {
        IERC20 Token;
        uint256 weight;
        uint256 lastMintBlock;
        uint256 mintedFeatherPerShare;
    }

    IERC20 public mintedFeather;

    address public rooster;

    address public rewardDistributor;

    uint256 public feathersPerBlock;

    NestControlMigrator public migrator;


    NestData[] public nestData;

    mapping(address => bool) public TokenExistsInNest;

    mapping(uint256 => mapping(address => Avian)) public depositor;

    uint256 public totalWeight = 0;

    uint256 public startBlock;

    uint256 public halvePeriod = 178560;
    uint256 public minimumFeathersPerBlock = 106867904178730000;

    uint256 public lastHalveBlock;

    event Deposit(address indexed user, uint256 indexed nestid, uint256 amount);
    event Withdraw(address indexed user, uint256 indexed nestid, uint256 amount);
    event ExitPoolDisgracefully(address indexed user, uint256 indexed nestid, uint256 amount);
    event Halve(uint256 newFeathersPerBlock, uint256 nextHalveBlockNumber);

    constructor( IERC20 _mintedFeather, address _rooster, uint256 _startBlock) public {
        mintedFeather = _mintedFeather;
        rooster = _rooster;
        feathersPerBlock = 216737791218638000;
        startBlock = _startBlock;
        lastHalveBlock = _startBlock;
    }

    function doHalvingCheck(bool _withUpdate) public {
        if (feathersPerBlock <= minimumFeathersPerBlock) {
            return;
        }
        bool doHalve = block.number > lastHalveBlock + halvePeriod;
        if (!doHalve) {
            return;
        }
        uint256 newFeathersPerBlock = feathersPerBlock.mul(96).div(100);
        if (newFeathersPerBlock >= minimumFeathersPerBlock) {
            feathersPerBlock = newFeathersPerBlock;
            lastHalveBlock = block.number;
            emit Halve(newFeathersPerBlock, block.number + halvePeriod);

            if (_withUpdate) {
                massUpdateNests();
            }
        }
    }

    function nestDepth() external view returns (uint256) {
        return nestData.length;
    }

    function add(uint256 _weight, IERC20 _Token) public onlyOwner {
        require(!TokenExistsInNest[address(_Token)], "Token Address already exists in nest");

        massUpdateNests();
        uint256 lastMintBlock = block.number > startBlock ? block.number : startBlock;
        totalWeight = totalWeight.add(_weight);
        nestData.push(
            NestData({
                Token: _Token,
                weight: _weight,
                lastMintBlock: lastMintBlock,
                mintedFeatherPerShare: 0
            })
        );
        TokenExistsInNest[address(_Token)] = true;
    }

    function updateTokenExists(address _TokenAddr, bool _isExists) external onlyOwner {
        TokenExistsInNest[_TokenAddr] = _ilastMintBlocksExists;
    }

    function set(uint256 _nestid, uint256 _weight) public onlyOwner {
        massUpdateNests();
        totalWeight = totalWeight.sub(nestData[_nestid].weight).add( _weight);
        nestData[_nestid].weight = _weight;
    }

    function setMigrator(NestControlMigrator _migrator) public onlyOwner {
        migrator = _migrator;
    }

    function migrate(uint256 _nestid) public onlyOwner {
        require(address(migrator) != address(0), "Address of migrator is null");

        NestData storage nest = nestData[_nestid];
        IERC20 Token = nest.Token;
        uint256 bal = Token.balanceOf(address(this));
        Token.safeApprove(address(migrator), bal);
        IERC20 newToken = migrator.migrate(Token);

        require(!TokenExistsInNest[address(newToken)], "New Token Address already exists in nest");
        require(bal == newToken.balanceOf(address(this)), "New Token balance incorrect");

        nest.Token = newToken;

        TokenExistsInNest[address(newToken)] = true;
    }

    function setRewardDistributor(address _newRewardDistributor) public onlyOwner {
        rewardDistributor = _newRewardDistributor;
    }

    function setRooster(address _newRooster) public onlyOwner {
        rooster = _newRooster;
    }

    function pendingMint(uint256 _nestid, address _user) external view returns (uint256) {
        NestData storage nest = nestData[_nestid];
        Avian storage user = depositor[_nestid][_user];
        uint256 mintedFeatherPerShare = nest.mintedFeatherPerShare;
        uint256 nestedTokenSupply = nest.Token.balanceOf(address(this));
        if (block.number > nest.lastMintBlock && nestedTokenSupply != 0) {
            uint256 blockPassed = block.number.sub(nest.lastMintBlock);
            uint256 tokenMint = blockPassed
                .mul(feathersPerBlock)
                .mul(nest.weight)
                .div(totalWeight);
            mintedFeatherPerShare = mintedFeatherPerShare.add(
                tokenMint.mul(1e12).div(nestedTokenSupply)
            );
        }
        return
          user.amount.mul(mintedFeatherPerShare).div(1e12).sub(user.mintDebt);
    }

    function massUpdateNests() public {
        uint256 length = nestData.length;
        for (uint256 nestid = 0; nestid < length; ++nestid) {
            updateNest(nestid);
        }
    }

    function updateNest(uint256 _nestid) public {
        doHalvingCheck(false);
        NestData storage nest = nestData[_nestid];
        if (block.number <= nest.lastMintBlock) {
            return;
        }

        uint256 nestedTokenSupply = nest.Token.balanceOf(address(this));
        if (nestedTokenSupply == 0) {
            nest.lastMintBlock = block.number;
            return;
        }
        uint256 blockPassed = block.number.sub(nest.lastMintBlock);
        uint256 tokenMint = blockPassed
            .mul(feathersPerBlock)
            .mul(nest.weight)
            .div(totalWeight);

        uint256 roosterMint = tokenMint.div(10);
        mintedFeather.mint(rooster, roosterMint);

        uint256 contractReserve = tokenMint.mul(2).div(5);
        mintedFeather.mint(address(this), contractReserve);

        uint256 votingReward = tokenMint.div(2);
        mintedFeather.mint(rewardDistributor, votingReward);

        nest.mintedFeatherPerShare = nest.mintedFeatherPerShare.add(
            contractReserve.mul(1e12).div(nestedTokenSupply)
        );
        nest.lastMintBlock = block.number;
    }

    function deposit(uint256 _nestid, uint256 _amount) public {
        NestData storage nest = nestData[_nestid];
        Avian storage user = depositor[_nestid][msg.sender];
        updateNest(_nestid);
        if (user.amount > 0) {
            uint256 pending = user
                .amount
                .mul(nest.mintedFeatherPerShare)
                .div(1e12)
                .sub(user.mintDebt);
            safeMintedTransfer(msg.sender, pending);
        }
        nest.Token.safeTransferFrom(address(msg.sender), address(this), _amount);
        user.amount = user.amount.add(_amount);
        user.mintDebt = user.amount.mul(nest.mintedFeatherPerShare).div(1e12);
        emit Deposit(msg.sender, _nestid, _amount);
    }

    function withdraw(uint256 _nestid, uint256 _amount) public {
        NestData storage nest = nestData[_nestid];
        Avian storage user = depositor[_nestid][msg.sender];
        require(user.amount >= _amount, "Insufficient Amount to withdraw");
        updateNest(_nestid);
        uint256 pending = user.amount.mul(nest.mintedFeatherPerShare).div(1e12).sub(user.mintDebt);
        safeMintedTransfer(msg.sender, pending);
        user.amount = user.amount.sub(_amount);
        user.mintDebt = user.amount.mul(nest.mintedFeatherPerShare).div(1e12);
        nest.Token.safeTransfer(address(msg.sender), _amount);
        emit Withdraw(msg.sender, _nestid, _amount);
    }

    function exitPoolDisgracefully(uint256 _nestid) public {
        NestData storage nest = nestData[_nestid];
        Avian storage user = depositor[_nestid][msg.sender];
        nest.Token.safeTransfer(address(msg.sender), user.amount);
        emit ExitPoolDisgracefully(msg.sender, _nestid, user.amount);
        user.amount = 0;
        user.mintDebt = 0;
    }

    function safeMintedTransfer(address _to, uint256 _amount) internal {
        uint256 mintBalance = mintedFeather.balanceOf(address(this));
        if (_amount > mintBalance) {
            mintedFeather.transfer(_to, mintBalance);
        } else {
            mintedFeather.transfer(_to, _amount);
        }
    }
}
